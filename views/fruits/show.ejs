<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fruit <%= fruit.name %></title>
  </head>
  <body>
    <%- include('../partials/nav.ejs') %>

    <h1><%= fruit.name %></h1>
    <% if(fruit.isReadyToEat){ %>
    <p>This fruit is ready to eat!</p>
    <% } else { %>
    <p>This fruit is not ripe yet! Check back tomorrow</p>
    <% } %>
    <form action="/fruits/<%=fruit._id%>?_method=DELETE" method="POST">
      <button type="submit">Delete <%= fruit.name %></button>
    </form>
    <a href="/fruits/<%=fruit.id%>/edit">Edit <%= fruit.name %></a>
  </body>
</html>
